name: 'Sous Media'
description: 'A recipe that contains modules and configuration for the Sous media experience.'
type: 'Site'
install:
  # Core modules
  - field_group
  - file
  - image
  - link
  - media
  - media_library
  - taxonomy
  - views
  # Contrib modules
  - dropzonejs
  - focal_point
  - media_library_edit
config:
  import:
    # Core.
    image: '*'
    media: '*'
    media_library: '*'
    # Contrib.
    focal_point: '*'